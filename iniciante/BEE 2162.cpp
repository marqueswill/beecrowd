#include <iostream>

using namespace std;

int main() {
    int n, padrao = 1;
    cin >> n;
    int paisagem[n];

    for (int i = 0; i < n - 2; i++) {
        scanf("%d", &paisagem[i]);
    }

    if (paisagem[0] < paisagem[1]) {
        for (int i = 0; i < n; i += 2) {
            if (!(paisagem[i] ))
        }
    }

    return 0;
}